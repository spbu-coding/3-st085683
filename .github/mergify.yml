pull_request_rules:
  - name: Ready to test
    conditions:
      - base=main
      - -title~=(?i)wip
      - label!=work-in-progress
      - -draft
      - -conflict
      - status-success=build
    actions:
      merge:
          method: merge

